<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
 "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="BLOGS">
	<!-- 为类型定义别名 -->
	<typeAlias alias="baseBlog" type="cn.seu.bingluo.entity.Blog" />
	<typeAlias alias="blog" type="cn.seu.bingluo.entity.BlogPojo" />
	<!-- 定义resultMap,将query结果映射为bean -->
	<resultMap id="BaseBlogResultMap" class="baseBlog">
		<result property="blogId" column="BLOGID"/>
		<result property="title" column="TITLE"/>
		<result property="content" column="CONTENT"/>
		<result property="postTime" column="POSTTIME"/>
		<result property="authorId" column="AUTHORID"/>
		<result property="tags" column="TAGS" nullValue=""/>
		<result property="folderId" column="FOLDERID"/>
		<result property="commentCount" column="COMMENT_COUNT"/>
		<result property="clickTimes" column="CLICKTIMES"/>
		<result property="updateTime" column="UPDATE_TIME"/>
	</resultMap>
	<resultMap id="BlogResultMap" class="blog">
		<result property="blogId" column="BLOGID"/>
		<result property="title" column="TITLE"/>
		<result property="content" column="CONTENT"/>
		<result property="postTime" column="POSTTIME"/>
		<result property="authorId" column="AUTHORID"/>
		<result property="authorName" column="USERNAME"/>
		<result property="tags" column="TAGS" nullValue=""/>
		<result property="folderId" column="FOLDERID"/>
		<result property="commentCount" column="COMMENT_COUNT"/>
		<result property="clickTimes" column="CLICKTIMES"/>
		<result property="updateTime" column="UPDATE_TIME"/>
	</resultMap>

	<select id="selectBlogsByBaseAndRange" resultMap="BlogResultMap" parameterClass="java.util.Map">
		<![CDATA[
		SELECT * FROM
			(SELECT a.blogId,a.title,a.content,a.postTime,b.userName,a.authorId,a.tags,a.folderId,a.COMMENT_COUNT,a.clickTimes,a.UPDATE_TIME
			 FROM BLOGS a,USERS b
			 WHERE a.authorId = b.userId
			 ORDER BY a.postTime DESC) c
		limit #base#,#range#
		]]>
	</select>

	<select id="selectBlogsByFolderId" resultMap="BlogResultMap" parameterClass="long">
		<![CDATA[
		SELECT a.blogId,a.title,a.content,a.postTime,b.userName,a.authorId,a.tags,a.folderId,a.COMMENT_COUNT,a.clickTimes,a.UPDATE_TIME
		FROM BLOGS a,USERS b
		WHERE a.folderId = #folderId# AND a.authorId = b.userId
		ORDER BY a.postTime DESC
		]]>
	</select>

	<select id="selectBlogsByBaseAndRangeAndTagName" resultMap="BlogResultMap" parameterClass="java.util.Map">
		<![CDATA[
		SELECT * FROM
			(SELECT a.blogId,a.title,a.content,a.postTime,b.userName,a.authorId,a.tags,a.folderId,a.COMMENT_COUNT,a.clickTimes,a.UPDATE_TIME
			 FROM BLOGS a, USERS b
			 WHERE instr(upper(a.tags), upper(#tagName#))>0 AND a.authorId = b.userId
			 ORDER BY a.postTime DESC) c
		limit #base#,#range#
		]]>
	</select>

	<select id="selectBlogsCountByUserId" resultClass="int" parameterClass="long">
		<![CDATA[
		SELECT count(*) FROM BLOGS WHERE authorId=#value#
		]]>
	</select>

	<select id="selectAllBlogsCount" resultClass="int">
		<![CDATA[
		SELECT count(*) FROM BLOGS
		]]>
	</select>

	<select id="selectFolderBlogsCount" resultClass="int" parameterClass="long">
		<![CDATA[
		SELECT count(*)
		FROM BLOGS
		WHERE folderId = #value#
		]]>
	</select>

	<select id="selectTagBlogsCount" resultClass="int" parameterClass="java.lang.String">
		<![CDATA[
		SELECT count(*)
		FROM BLOGS
		WHERE instr(tags, #value#)>0
		]]>
	</select>

	<select id="selectBlogById" resultMap="BlogResultMap" parameterClass="long">
		<![CDATA[
		SELECT a.blogId,a.title,a.content,a.postTime,b.userName,a.authorId,a.tags,a.folderId,a.COMMENT_COUNT,a.clickTimes,a.UPDATE_TIME
		FROM BLOGS a,USERS b
		WHERE a.blogId = #value# AND a.authorId = b.userId
		]]>
	</select>

	<insert id="insertBlog" parameterClass="baseBlog">
		<![CDATA[
		INSERT INTO BLOGS
		(title,content,postTime,authorId,tags,folderId,COMMENT_COUNT,clickTimes,UPDATE_TIME)
		VALUES (#title#,#content#,current_timestamp(),#authorId#,#tags#,#folderId#,#commentCount#,#clickTimes#,current_timestamp())
		]]>
		<selectKey resultClass="long" keyProperty="blogId">
			select last_insert_id() as ID from BLOGS limit 1
		</selectKey>
	</insert>

	<update id="updateBlog" parameterClass="blog">
		<![CDATA[
		UPDATE BLOGS
		SET	title = #title#,
			content = #content#,
			tags = #tags#,
			folderId = #folderId#,
			UPDATE_TIME = current_timestamp()
		WHERE blogId = #blogId#
		]]>
	</update>

	<update id="addClickTime" parameterClass="long">
		<![CDATA[
		UPDATE BLOGS
		SET	clickTimes = clickTimes+1
		WHERE blogId = #value#
		]]>
	</update>

	<select id="selectHotBlogs" resultMap="BlogResultMap">
		<![CDATA[
		SELECT * FROM(
			SELECT a.blogId,a.title,a.content,a.postTime,b.userName,a.authorId,a.tags,a.folderId,a.COMMENT_COUNT,a.clickTimes,a.UPDATE_TIME
			FROM BLOGS a,USERS b
			WHERE a.authorId = b.userId
			ORDER BY a.COMMENT_COUNT DESC) c
		limit 0,5
		]]>
	</select>
	
	<delete id="deleteBlogById" parameterClass="long">
		<![CDATA[
		delete from BLOGS where BLOGID = #value#
		]]>
	</delete>
</sqlMap>
